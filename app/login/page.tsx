"use client";
import {FormEvent,useEffect,useState} from "react";
import {useRouter} from "next/navigation";
export default function Login(){const router=useRouter(),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[show,setShow]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState(""),[returning,setReturning]=useState(false);useEffect(()=>{setReturning(localStorage.getItem("shift-zone-has-logged-in")==="1")},[]);async function submit(event:FormEvent){event.preventDefault();setBusy(true);setError("");try{const response=await fetch("/api/auth",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,password})}),data=await response.json();if(!response.ok)throw new Error(data.error||"Não foi possível entrar");localStorage.setItem("shift-zone-has-logged-in","1");router.replace("/")}catch(err){setError(err instanceof Error?err.message:"Não foi possível entrar")}finally{setBusy(false)}}return <main className="loginPage"><section className="loginShell"><div className="loginBrand"><div><div className="loginLogoCrop" role="img" aria-label="Logo da Shift Zone Finance"/><h1><span>Shift Zone</span></h1><strong>Desenvolvido por Robson Antunes</strong></div></div><form className="loginCard" onSubmit={submit}><div className="loginCardHead"><span>● ÁREA PROTEGIDA</span><h2>{returning?"Bem-vindo de volta":"Bem-vindo"}</h2><p>Entre para acessar o painel financeiro.</p></div><label>E-mail<input type="email" autoComplete="email" autoCapitalize="none" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Digite seu e-mail" required/></label><label>Senha<div className="passwordField"><input type={show?"text":"password"} autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Digite a senha" required/><button type="button" onClick={()=>setShow(v=>!v)} aria-label={show?"Ocultar senha":"Mostrar senha"}>{show?"Ocultar":"Mostrar"}</button></div></label>{error&&<div className="loginError" role="alert">{error}</div>}<button className="loginSubmit" disabled={busy}>{busy?"Entrando…":"Entrar no dashboard"}<span>→</span></button><small><a href="/cadastro">Criar conta</a><br/><a href="/recuperar-senha">Esqueci minha senha</a></small></form></section></main>}
