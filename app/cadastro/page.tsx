"use client";
import {FormEvent,useState} from "react";
export default function Cadastro(){const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[confirm,setConfirm]=useState(""),[busy,setBusy]=useState(false),[message,setMessage]=useState(""),[error,setError]=useState("");async function submit(event:FormEvent){event.preventDefault();setError("");setMessage("");if(password!==confirm)return setError("As senhas não coincidem");setBusy(true);try{const response=await fetch("/api/auth/signup",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,password})}),data=await response.json();if(!response.ok)throw new Error(data.error);setMessage(data.message)}catch(err){setError(err instanceof Error?err.message:"Não foi possível criar a conta")}finally{setBusy(false)}}return <main className="loginPage"><section className="loginShell"><div className="loginBrand"><div><div className="loginLogoCrop" role="img" aria-label="Logo da Shift Zone"/><h1><span>Shift Zone</span></h1></div></div><form className="loginCard" onSubmit={submit}><div className="loginCardHead"><span>● NOVA CONTA</span><h2>Comece sua organização</h2><p>Seus dados, protegidos e só seus.</p></div><label>E-mail<input type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Digite seu e-mail" required/></label><label>Senha<input type="password" autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Mínimo de 8 caracteres" minLength={8} required/></label><label>Confirmar senha<input type="password" autoComplete="new-password" value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="Digite novamente" minLength={8} required/></label>{error&&<div className="loginError" role="alert">{error}</div>}{message&&<div className="loginError" role="status">{message}</div>}<button className="loginSubmit" disabled={busy}>{busy?"Criando…":"Criar minha conta"}<span>→</span></button><small>Já tem uma conta? <a href="/login">Entrar</a></small></form></section></main>}
